{
  "name": "susanoo-chat",
  "version": "1.0.0",
  "description": "Interface de chat moderne full-page (thème Susanoo) + bridge live vers Hashirama.",
  "type": "module",
  "main": "dist/server.js",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "npm run build:backend && npm run build:frontend",
    "build:backend": "tsc",
    "build:frontend": "esbuild frontend/script.ts --bundle --outfile=dist/script.js --target=es2020 --format=iife",
    "start": "node dist/server.js",
    "type-check": "tsc --noEmit"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^25.3.0",
    "esbuild": "^0.27.3",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
