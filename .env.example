# Configuration SSH pour accès distant au conteneur dev-workspace (développement local)
VPS_HOST=87.106.22.224
VPS_USER=root
VPS_SSH_KEY=/path/to/your/ssh/key

# Ou utiliser l'alias SSH configuré dans ~/.ssh/config
VPS_SSH_ALIAS=vps

# Mode: development (utilise SSH) ou production (utilise Docker local)
NODE_ENV=development
