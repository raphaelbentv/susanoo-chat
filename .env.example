# Mode: development (utilise SSH) ou production (utilise Docker local)
NODE_ENV=development

# SSH (développement local uniquement)
VPS_SSH_ALIAS=vps

# REQUIRED — Passphrase pour chiffrer les backups et les sessions
# Générer avec : node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
BACKUP_PASSPHRASE=

# TLS — Chemins vers certificat et clé privée (optionnel, recommandé en prod)
# TLS_CERT_PATH=/etc/letsencrypt/live/domain/fullchain.pem
# TLS_KEY_PATH=/etc/letsencrypt/live/domain/privkey.pem

# CORS — Origine autorisée (défaut: * — restreindre en prod)
# ALLOWED_ORIGIN=https://chat.example.com

# SMTP — Configuration email (optionnel, pour envoi des identifiants)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=
# SMTP_PASS=
# SMTP_FROM_NAME=Hashirama
# SMTP_FROM_EMAIL=noreply@example.com
